import React from 'react';

function AuthorInfo(props) {
  // we are passing in authorId

  // fetch author public info - image, name, title, total upvotes

  return (
    <div>
      {/* display author public info */}
    </div>
  )
};

export default AuthorInfo;