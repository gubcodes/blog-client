import { React, useState } from 'react';

function ProfileAuthorPortal(props) {
  const [articles, setArticles] = useState([]);

  // we'll set the fetch for the articles they have written and set the state above

  return (
    <div>
      {/* .map() over the articles here */}
    </div>
  )
};

export default ProfileAuthorPortal;