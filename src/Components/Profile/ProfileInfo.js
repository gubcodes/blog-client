import React from 'react';

function ProfileInfo(props) {
  const [name, setName] = useState('');

  // we'll set the fetch for the user's info and set the states above

  return (
    <div>
      
    </div>
  )
};

export default ProfileInfo;