import React from 'react';

function ArticleBlockCodePen(props) {
  // props.blockContent was passed in

  // we'll need to get the following from the URL passed in: user="gubcode" hash="OJbVVLq"
  // we also need to allow the user to select the height and default tab
  // so we should save the codepen in the editor as an array of objects: [{url: ""}, {height: ""}, {tab: ","}]

  // see "react-codepen-embed" in blog project bookmarks
  return (
    <div>
      
    </div>
  )
};

export default ArticleBlockCodePen;